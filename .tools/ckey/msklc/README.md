The kbd.h file is a modified version so caps-lock is remapped to ctrl.
It has to be placed into Program Files (x86)/Microsoft Keyboard Layour Creator/inc

The kbd.h.backup is a backup file to restore the default behavior.